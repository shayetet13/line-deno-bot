# dependencies
node_modules/

# build output
dist/
build/
*.tsbuildinfo

# coverage
coverage/
*.lcov

# env / secrets
.env
.env.*
!.env.example

# LINE account sessions — these ARE the credentials. Never commit.
.sessions/
*.linejs.json

# local launcher config: server host + ssh credentials
start.local.ps1

# deploy ssh keys kept untracked in the project root
/*_ed25519
!/*_ed25519.pub
/*.pem

# real bot configs name private rooms and senders; only the example is tracked
config/bots/*.json
!config/bots/*.example.json

# local databases
*.sqlite
*.sqlite-shm
*.sqlite-wal
.control/users.json
.control/users.json.tmp-*
.control/bot-users/
.control/instances/
.control/ssh/
.claude/

# logs
logs/
*.log

# editor / OS
.DS_Store
.idea/
.vscode/
Thumbs.db
desktop.ini

# runtime traces (may contain room ids)
bench/**/out/
traces/
